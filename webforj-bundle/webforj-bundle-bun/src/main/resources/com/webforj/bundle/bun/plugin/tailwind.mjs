import plugin from 'bun-plugin-tailwind';

// bun-plugin-tailwind default-exports a ready Bun.build plugin. webforJ loads each wrapper as a
// factory, so it is returned as is. The stylesheet that imports tailwindcss and declares the source
// scan is generated by the Tailwind bundle registrar.
const tailwindPlugin = () => plugin;

export default tailwindPlugin;

<?xml version="1.0"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xmlns="http://maven.apache.org/POM/4.0.0"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.webforj</groupId>
    <artifactId>webforj-minify</artifactId>
    <version>25.10-SNAPSHOT</version>
  </parent>

  <artifactId>webforj-minify-common</artifactId>
  <name>${project.artifactId}</name>
  <description>Shared constants, interfaces, and utilities for webforJ asset minification</description>

  <dependencies>
    <!-- CSS minification (optional, for default implementation) -->
    <dependency>
      <groupId>com.helger</groupId>
      <artifactId>ph-css</artifactId>
      <optional>true</optional>
    </dependency>

    <!-- JavaScript minification (optional, for default implementation) -->
    <dependency>
      <groupId>com.google.javascript</groupId>
      <artifactId>closure-compiler</artifactId>
      <optional>true</optional>
    </dependency>

    <!-- JSON processing -->
    <dependency>
      <groupId>com.google.code.gson</groupId>
      <artifactId>gson</artifactId>
    </dependency>

    <!-- Testing -->
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>
</project>
